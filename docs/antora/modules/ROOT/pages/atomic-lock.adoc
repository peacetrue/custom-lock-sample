= 原子性锁

原子性锁通过 `CAS` 保证线程安全，循环抢锁实现阻塞等待以及锁释放后抢锁。

实现代码如下：

.AtomicLock
[source%nowrap,java]
----
include::{sourcedir}/com/github/peacetrue/sample/lock/AtomicLock.java[tag=class]
----

测试代码如下：

.CounterTest
[source%nowrap,java]
----
include::{testsourcedir}/com/github/peacetrue/sample/lock/CounterTest.java[tags=class-start;testCustomLock;class-end;]
----

`AtomicLock` 解决了并发带来的问题，但也存在一些问题。

`Counter.increase` 执行时间长，导致 CPU 利用率过高：

.CounterTest
[source%nowrap,java]
----
include::{testsourcedir}/com/github/peacetrue/sample/lock/CounterTest.java[tags=class-start;atomicLockInSlowCase;class-end;]
----

16 个核心利用率全部达到 100%：

image::image-2021-06-01-05-50-11-668.png[]


